# Copy to .env.local locally or configure in Vercel Project Settings.
# Never commit real API keys.

SARVAM_API_KEY=

# Optional future voice settings. Keep provider calls server-side.
SARVAM_TTS_MODEL=
SARVAM_STT_MODEL=
